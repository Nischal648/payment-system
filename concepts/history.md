history of programming 
-> machine language:0/1 - cpu executes it directly
  -prone to error 
  -not scalable

->assembly level language : use NUMMOICS (uses english keyword)
  -tightly coverd with hardware 
  -instruction execute one by one
  -not scalable directly dealing with the hardware
  -tedious
  -no loops and no methods  
 
 ->procedural programming language
   -introduced fuction loops blocks switch case and many more 
   -not able to solve complex problem 
   -code was executed in instructions 
   -ex:c
   -do not able to solve complex problem

   so thats why we shift to object oriented programming 

 -> oops
   -real world modelling 
   -data security
   -sclable/reusable application

   -in real world object interact with each other 

   ->objects has 2 things 
     -characterstics and behaviour(functions)
         |                    |
        -engine              -start()
        -brand               -stop()
        -model               -gearshift() 
        -wheels              -accelearate()

 -> pillars of oops   
   -abstraction
   -polymorphism
   -inheritance 
   -polymorphism

   ->abstraction 
     -in real life the object exist is abstracted form means some data is hidden , it does to require to know about of the object 

     -for example -> when you drive the car you use accelartor,break,gear,clutch then and you do not need to know how everything is working internally 

     -so abstraction unncessary details from the client(the one who is using the object ) and showcase only what is necessaryf